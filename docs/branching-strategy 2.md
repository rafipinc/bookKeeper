# Branching Strategy

- `main` is the production and integration branch.
- Feature branches are short-lived and cut from `main`.
- Pull requests target `main`.
- Squash merge is the only intended merge style.
- Coding agents must not merge to `main` without Rafi's explicit instruction.

See [GitHub Flow Governance](./github-flow-governance.md) for branch protection and repository settings.
